package aa;

import java.util.HashMap;
import java.util.Map;

/**
 * See the image Problem.png for the problem description.
 */
public class ArchitectureAnalytics {

	/**
	 * Count the visits to all services by each user
	 * @return a map of user ids and the number of visits to all services from the inputs.
	 */
    public Map<Long, Long> count(@SuppressWarnings("unchecked") Map<String, UserStats>... visits) 
    {
    	Map<Long,Long> all = new HashMap<>();
        return all;
    }
}